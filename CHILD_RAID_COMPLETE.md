# 🗡️ РЕЙДИ ДІТЕЙ - ПОВНА РЕАЛІЗАЦІЯ

**Дата:** 2 квітня 2026 р.  
**Версія:** 2.0  
**Статус:** ✅ ПОВНІСТЮ ПРАЦЮЄ

---

## 📋 ЩО БУЛО ДОДАНО

### 1. База даних

**Нова таблиця:**
```sql
CREATE TABLE child_raids (
    id SERIAL PRIMARY KEY,
    child_id BIGINT,
    user_id BIGINT,
    chat_id BIGINT,
    raid_type TEXT,
    reward INTEGER,
    start_time BIGINT,
    end_time BIGINT,
    claimed BOOLEAN DEFAULT FALSE
)
```

### 2. Функції БД

**Нові функції:**
- ✅ `send_child_onraid()` - зберігає рейд в БД
- ✅ `get_active_child_raid()` - отримує активний рейд
- ✅ `claim_child_raid()` - отримання нагороди

### 3. Команди бота

**Нові/Оновлені команди:**
- ✅ `/childraid` - відправка в рейд (з перевіркою кулдауну)
- ✅ `/childclaim` - отримання нагороди (НОВА!)
- ✅ `/children` - показує статус рейдів

---

## 🎮 ЯК ЦЕ ПРАЦЮЄ

### Крок 1: Відправка в рейд

```
/childraid 123 coins

🗡️ РЕЙД ВІДПРАВЛЕНО!

Дитина: Big-put-1
Тип рейду: COINS
Очікувана нагорода: 17,050 💰 монет
(Бонус: +520.6%)
Час рейду: 5 хв.

ID рейду: `42`
По завершенню: /childclaim 42
```

### Крок 2: Перевірка статусу

```
/children

👶 Твої діти:

1. `123` - **Big-put-1** ⚪ 🗡️ В рейді (3 хв)
   ⚖️ Вага: 556 кг
   🎂 Народжений: 02.04.2026
```

### Крок 3: Отримання нагороди

```
/childclaim 42

✅ РЕЙД ЗАВЕРШЕНО!

Дитина: Big-put-1
Нагорода: 💰 17,050 монет
Бонус: +520.6%

Дитина тепер може знову відправитись в рейд!
/childraid 123
```

---

## 📊 МЕХАНІКА

### Розрахунок нагороди:

```python
# Базова нагорода
base_reward = вага_дитини × 5

# Випадкове відхилення ±20%
reward = random.randint(base_reward × 0.8, base_reward × 1.2)

# Застосовуємо бонус від дітей
bonus_mult = 1 + (бонус_від_дітей / 100)
final_reward = int(reward × bonus_mult)
```

### Приклад:

```
Вага дитини: 556 кг
Base reward: 556 × 5 = 2,780
Random: 2,780 × 1.0 = 2,780 (середнє)
Бонус: +520.6% → × 6.206
Final: 2,780 × 6.206 = 17,252 монет!
```

### Час рейду:

```python
raid_time = max(300, 3600 - (вага × 10))

# 556 кг = 3600 - 5560 = -1960 → 300 сек (5 хв)
# 100 кг = 3600 - 1000 = 2600 сек (43 хв)
```

---

## 🎯 ТИПИ РЕЙДІВ

| Тип | Нагорода | Статус |
|-----|----------|--------|
| `coins` | 💰 Монети | ✅ Працює |
| `xp` | ⭐ XP | ✅ Працює |
| `items` | 🎁 Предмети | ⚠️ Частково (дає монети) |

---

## ⏰ КУЛДАУНИ

### Обмеження:

| Параметр | Значення |
|----------|----------|
| Час рейду | 5 хв - 1 год (залежить від ваги) |
| Одночасних рейдів | Необмежено (кожна дитина окремо) |
| Перевірка | Заборона якщо активний рейд |

### Перевірка:

```python
# При відправці
active_raid = get_active_child_raid(child_id, chat_id)
if active_raid and now < end_time:
    bot.reply_to("⏰ Дитина вже в рейді!")
    return
```

---

## 💡 ОСОБЛИВОСТІ

### 1. Бонуси від дітей

```
Застосовуються до нагороди:
- Базовий бонус: вага / 10
- Віковий бонус: +10% за день (макс. 30 днів)
- Мутації: 2x бонус

Приклад: +520.6% = ×6.206 до нагороди!
```

### 2. Спільне володіння

```
Співвласники можуть:
- ✅ Відправляти в рейд
- ✅ Отримувати нагороду
- ✅ Перевіряти статус
```

### 3. Відображення в /children

```
Індикатори:
🗡️ В рейді (X хв) - активний рейд
✅ Готовий до claim! - рейд завершився
(немає) - не в рейді
```

---

## 📈 ПРИКЛАДИ ДОХОДУ

### 9 дітей по 550 кг:

```
Один рейд:
9 × 2,750 × 6.2 = 153,450 монет

Час: 5 хвилин

За годину (12 рейдів):
12 × 153,450 = 1,841,400 монет! 💰
```

### З різними типами:

```
6 дітей coins: 6 × 17,050 = 102,300 монет
3 дітей xp: 3 × 17,050 = 51,150 XP

За 5 хвилин!
```

---

## 🔧 ТЕХНІЧНА ІНФОРМАЦІЯ

### Запити до БД:

**Збереження рейду:**
```sql
INSERT INTO child_raids 
  (child_id, user_id, chat_id, raid_type, reward, start_time, end_time)
VALUES 
  (?, ?, ?, ?, ?, ?, ?)
```

**Отримання активного:**
```sql
SELECT * FROM child_raids
WHERE child_id = ? AND chat_id = ? AND claimed = FALSE
ORDER BY end_time DESC LIMIT 1
```

**Claim нагороди:**
```sql
UPDATE child_raids SET claimed = TRUE WHERE id = ?
```

### Логіка claim:

```python
def claim_child_raid(raid_id, user_id, chat_id):
    # 1. Отримуємо рейд
    raid = get_raid(raid_id, user_id, chat_id)
    
    # 2. Перевіряємо чи завершився
    if now < end_time:
        return {'error': 'Рейд ще не завершився'}
    
    # 3. Позначаємо як отриманий
    update_claimed(raid_id)
    
    # 4. Видаємо нагороду
    if raid_type == 'coins':
        add_coins(user_id, final_reward)
    elif raid_type == 'xp':
        add_xp(user_id, final_reward)
    
    return {'success': True, 'reward': final_reward}
```

---

## ⚠️ МОЖЛИВІ ПРОБЛЕМИ

### Проблема: Рейд не завершується

**Рішення:**
```
1. Перевірте час: /children
2. Зачекайте завершення
3. /childclaim <ID рейду>
```

### Проблема: Нагорода не видається

**Рішення:**
```
1. Перевірте чи рейд завершився
2. Перевірте права (власник/співвласник)
3. /childclaim з правильним ID
```

### Проблема: Не можна відправити в рейд

**Рішення:**
```
1. Перевірте чи дитина не в рейді
2. /children - покаже статус
3. Зачекайте завершення або claim
```

---

## 🎯 МАЙБУТНІ ПОКРАЩЕННЯ

### Plan:

- [ ] Предмети з рейдів (items тип)
- [ ] Автоматичний claim через X годин
- [ ] Групові рейди (кілька дітей)
- [ ] Критичний успіх (2x нагорода)
- [ ] Рідкісні предмети з рейдів
- [ ] Історія рейдів

---

## 📊 ПІДСУМКОВА ТАБЛИЦЯ

| Функція | Статус | Примітки |
|---------|--------|----------|
| Таблиця child_raids | ✅ | Створена |
| send_child_on_raid | ✅ | Зберігає в БД |
| get_active_child_raid | ✅ | Працює |
| claim_child_raid | ✅ | Видає нагороду |
| /childraid | ✅ | З перевіркою |
| /childclaim | ✅ | Працює |
| /children (статус) | ✅ | Показує рейд |
| coins тип | ✅ | Повністю |
| xp тип | ✅ | Повністю |
| items тип | ⚠️ | Дає монети |
| Бонуси від дітей | ✅ | Застосовуються |

---

**ДАТА СТВОРЕННЯ:** 2 квітня 2026 р.  
**ВЕРСІЯ:** 2.0  
**СТАТУС:** ✅ ПОВНІСТЮ ПРАЦЮЄ
