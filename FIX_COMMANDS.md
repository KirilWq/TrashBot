# 🔧 Виправлення Проблем з Командами

## ✅ Виправлено:

### 1. **/help** - Виправлено синтаксис
**Проблема:** Пропущена зірочка в Markdown
**Вирішено:** Додано закриваючу зірочку `**Швидкі Меню:**`

## 📋 Перевірка Роботи Команд:

### 1. **Запустіть бота:**
```bash
cd d:\WEB\TGbot
python bot.py
```

### 2. **Перевірте логи:**
Якщо є помилки, перевірте `bot.log`

### 3. **Тестові команди:**
```
/help - має показати список команд
/menu - inline меню з кнопками
/guild_menu - меню гільдії
```

## 🔍 Якщо Команда Не Працює:

### Крок 1: Перевірте синтаксис
```bash
cd d:\WEB\TGbot
python -m py_compile bot.py
python -m py_compile db.py
```

### Крок 2: Перезапустіть бота
```
1. Зупиніть поточний процес (Ctrl+C)
2. python bot.py
```

### Крок 3: Перевірте змінні середовища
```
BOT_TOKEN=ваш_токен
DATABASE_URL=ваша_база
```

### Крок 4: Перевірте права бота
- Бот має бути в чаті
- Бот має права адміністратора

## 📝 Відомі Команди:

### Меню (5):
- `/guild_menu` - гільдія
- `/warriors_menu` - воїни
- `/items_menu` - предмети
- `/genetics_menu` - генетика
- `/trade_menu` - трейди

### Гільдія (20+):
- `/guild_territories` - території
- `/guild_warriors` - воїни
- `/guild_items` - предмети гільдії
- `/guild_claim_item` - вивести предмет
- `/guild_buy_warrior` - купити воїнів
- `/guild_defend` - захист
- `/guild_attack` - атака

### Предмети (9):
- `/inventory` - інвентар
- `/use_item` - використати предмет
- `/item_trade` - трейд
- `/guild_items` - гільдійні предмети

## 🎯 Швидкий Тест:

1. Напишіть `/help` - має показати список
2. Напишіть `/guild_menu` - має показати меню
3. Напишіть `/inventory` - має показати інвентар

## ❌ Помилки:

| Помилка | Причина | Вирішення |
|---------|---------|-----------|
| "Command not found" | Бот не запущений | Запустіть `python bot.py` |
| "Database error" | Немає DATABASE_URL | Додайте в .env |
| "Token error" | Немає BOT_TOKEN | Додайте в .env |

## 📞 Підтримка:

Якщо проблеми залишаються:
1. Перевірте `bot.log`
2. Перевірте синтаксис `python -m py_compile bot.py`
3. Перезапустіть бота
