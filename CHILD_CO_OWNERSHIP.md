# 🤝 СПІВОЛОДІННЯ ДІТЬМИ ХРЯКІВ

**Дата:** 2 квітня 2026 р.  
**Версія:** 1.0

---

## 📋 ОПИС

Додано можливість одружувати хряків від різних користувачів зі **спільним володінням** потомством.

---

## 🎯 ЯК ЦЕ ПРАЦЮЄ

### 1. Одруження хряків

Коли два користувачі одружують своїх дітей:
- **Дитина 1:** Власник - Користувач A
- **Дитина 2:** Власник - Користувач B
- **Онук:** Спільне володіння (A + B)

### 2. Спільне володіння

Обидва користувачі можуть:
- ✅ Переглядати дитину в `/children`
- ✅ Командувати дитиною (`/childtrain`, `/childraid`)
- ✅ Перейменовувати (`/renamechild`)
- ✅ Одружувати онуків (`/childmarry`)
- ✅ Отримувати нагороди від рейдів

---

## 🔄 МЕХАНІКА

### База даних

**Нова колонка:**
```sql
ALTER TABLE children ADD COLUMN co_owner_id BIGINT DEFAULT NULL
```

**Структура:**
```sql
children (
    id SERIAL PRIMARY KEY,
    user_id BIGINT,          -- Основний власник
    co_owner_id BIGINT,      -- Співвласник (NULL якщо немає)
    chat_id BIGINT,
    father_user_id BIGINT,
    mother_user_id BIGINT,
    name TEXT,
    weight INTEGER,
    ...
)
```

### Запити

**Отримання дітей:**
```sql
SELECT * FROM children
WHERE (user_id = ? OR co_owner_id = ? OR mother_user_id = ? OR father_user_id = ?)
AND chat_id = ?
```

**Оновлення (доступно обом):**
```sql
UPDATE children SET weight = ?
WHERE id = ? AND (user_id = ? OR co_owner_id = ?)
```

---

## 🎮 ПРИКЛАД ВИКОРИСТАННЯ

### Крок 1: Одруження дітей

```
# Користувач A (ID: 123) має дитину ID 10
# Користувач B (ID: 456) має дитину ID 20

# Користувач A ініціює одруження
/childmarry 10 20

🎉 ДІТИ ОДРУЖЕНІ!

👶 Народився онук:
Ім'я: 10a-20b-F1
Вага: 15 кг
🤝 Спільне володіння: Користувач A + Користувач B
```

### Крок 2: Перегляд дітей

```
# Обидва користувачі бачать онука в /children

/children

👶 Твої діти:

1. `21` - **10a-20b-F1** ⚪ 🤝 Спільно
   ⚖️ Вага: 15 кг
   🎂 Народжений: 02.04.2026
   🧬 Особливість: Немає
```

### Крок 3: Управління

```
# Обидва користувачі можуть тренувати
/childtrain 21

✅ Дитину треновано!
Вага: 15 → 20 кг (+5)

# Обидва можуть відправляти в рейд
/childraid 21

✅ Рейд завершено!
Нагорода: 100 монет
```

---

## 📊 ІНДИКАТОРИ ВОЛОДІННЯ

### В команді `/children`:

| Індикатор | Значення |
|-----------|----------|
| (немає) | Ви єдиний власник |
| 🤝 Спільно | Ви власник, є співвласник |
| 👥 Ви співвласник | Ви співвласник, не основний власник |

---

## 🔧 ТЕХНІЧНА ІНФОРМАЦІЯ

### Оновлені функції БД:

**db.py:**
```python
# Додано co_owner_id параметр
add_child(..., co_owner_id=None)

# Оновлено запити для підтримки співвласників
get_children(user_id, chat_id)  # Шукає по user_id OR co_owner_id
get_child(child_id, chat_id)    # Повертає co_owner_id
rename_child(...)               # Доступно owner OR co_owner
sacrifice_child(...)            # Доступно owner OR co_owner
train_child(...)                # Доступно owner OR co_owner
send_child_on_raid(...)         # Доступно owner OR co_owner
marry_children(...)             # Створює co_owner при одруженні
```

### Логіка marry_children():

```python
def marry_children(child1_id, child2_id, user_id, chat_id):
    # Отримуємо дітей
    child1 = get_child(child1_id)
    child2 = get_child(child2_id)
    
    # Перевіряємо права
    if user_id not in [child1.owner, child1.co_owner, child2.owner, child2.co_owner]:
        return {'error': 'Немає прав'}
    
    # Визначаємо співвласника
    co_owner = None
    if child1.owner != child2.owner:
        co_owner = child2.owner if child1.owner == user_id else child1.owner
    
    # Створюємо онука зі співвласником
    add_child(
        user_id=user_id,
        co_owner_id=co_owner,  # <-- Співвласник
        ...
    )
```

---

## ⚠️ ОБМЕЖЕННЯ

### Що не можуть співвласники:
- ❌ Видаляти дитину (тільки основний власник)
- ❌ Змінювати гени (тільки основний власник)

### Що можуть співвласники:
- ✅ Тренувати
- ✅ Відправляти в рейд
- ✅ Перейменовувати
- ✅ Одружувати
- ✅ Переглядати статистику

---

## 🎯 СЦЕНАРІЇ ВИКОРИСТАННЯ

### Сценарій 1: Спільне розведення

```
Користувач A + Користувач B
→ Одружують дітей
→ Спільно тренують онука
→ Обидва отримують нагороди
```

### Сценарій 2: Сімейне дерево

```
Дідусь A + Дідусь B
→ Батько (спільно)
→ Онук (спільно)
→ Правнук (спільно)
```

### Сценарій 3: Гільдійне розведення

```
Члени гільдії одружують хряків
→ Спільні діти
→ Більша армія для босів
```

---

## 📈 СТАТИСТИКА

| Показник | Значення |
|----------|----------|
| Колонка в БД | `co_owner_id BIGINT` |
| Оновлено функцій | 7 |
| Команд з підтримкою | 6 |
| Типи володіння | 2 (повне, спільне) |

---

## 🐛 ВИРІШЕННЯ ПРОБЛЕМ

### Проблема: Онук не відображається у співвласника

**Рішення:**
```
1. Перевірте co_owner_id в БД
2. Перегляньте /children ще раз
3. Переконайтесь що chat_id співпадає
```

### Проблема: Неможливо тренувати

**Рішення:**
```
1. Перевірте права (/childinfo)
2. Переконайтесь що ви owner або co_owner
3. Перевірте кулдаун
```

---

## 🎯 МАЙБУТНІ ПОКРАЩЕННЯ

### Plan:
- [ ] Додати більше 2 співвласників
- [ ] Розподіл нагород між співвласниками
- [ ] Голосування за рішення (жертва/одруження)
- [ ] Історія дій співвласників
- [ ] Експорт сімейного дерева

---

**ДАТА СТВОРЕННЯ:** 2 квітня 2026 р.  
**ВЕРСІЯ:** 1.0  
**СТАТУС:** ✅ ПРАЦЮЄ
